name: My Workflow
on:
  workflow_dispatch:
jobs:
  myJob:
    strategy:
      matrix:
        parentProject: ['parent1', 'parent2']
        childProject: ['child1', 'child2']
    runs-on: ubuntu-latest
    steps:
      - name: Do something
        run: echo "Working on parent project: ${{ matrix.parentProject }}, child project: ${{ matrix.childProject }}."
