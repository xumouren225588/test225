name: Build PaperMC Server and Push to ACR
on:
  workflow_dispatch:
    inputs:
      papermc_kernel:
        description: '请选择服务器核心版本'
        required: true
        type: choice
        
        default: '1.21.1'
        options:
          - '1.21.1'
          - '1.21'
          - '1.20'
      build_times:
        description: '请选择构建次数'
        required: true
        type: choice
        default: '10'
        options:
          - '10'
          - '20'
          - '30'
          - '40'
          - '50'

env:
  REGISTRY_URL: ${{ secrets.REGISTRY_URL }}
  REGISTRY_USERNAME: ${{ secrets.REGISTRY_USERNAME }}
  REGISTRY_PASSWORD: ${{ secrets.REGISTRY_PASSWORD }}

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Set selected options
        run: |
          echo "Selected PaperMC kernel: ${{ github.event.inputs.papermc_kernel }}"
          echo "Selected build times: ${{ github.event.inputs.build_times }}"

      - name: Download PaperMC Server
        run: |
          wget "https://api.papermc.io/v2/projects/paper/versions/${{ github.event.inputs.papermc_kernel }}/builds/${{ github.event.inputs.build_times }}/downloads/paper-${{ github.event.inputs.papermc_kernel }}-${{ github.event.inputs.build_times }}.jar"
          #https://api.papermc.io/v2/projects/paper/versions/1.21.1/builds/118/downloads/paper-1.21.1-118.jar
      - name: Generate Dockerfile
        run: |
          echo "FROM openjdk:17" > Dockerfile
          echo "ADD paper-${{ github.event.inputs.papermc_kernel }}-${{ github.event.inputs.build_times }}.jar /app/server.jar" >> Dockerfile
          echo "EXPOSE 25565" >> Dockerfile
          echo "CMD [\"java\", \"-Xms1G\", \"-Xmx2G\", \"-jar\", \"server.jar\"]" >> Dockerfile

      - name: Generate eula.txt
        run: |
          echo "eula=True" > eula.txt

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v4
        with:
          context: .
          registry: ${{ env.REGISTRY_URL }}
          username: ${{ env.REGISTRY_USERNAME }}
          password: ${{ env.REGISTRY_PASSWORD }}
          push: true
          tags: ${{ env.REGISTRY_URL }}/your-image-name:${{ github.event.inputs.papermc_kernel }}-${{ github.event.inputs.build_times }}
          
